export const InputBox = ({text, placeholder, icon}) => {
    return <div className="">
        <input type={text} className="h-10 w-80 border-1 rounded-2xl p-3" placeholder={placeholder} />
        
    </div>
}